#include <bits/stdc++.h>
using namespace std;

const int MAXN = 100000000;
unsigned char bitmap[MAXN / 8 + 1];

inline void setBit(int x) {
    bitmap[x >> 3] |= (1 << (x & 7));
}

inline bool getBit(int x) {
    return bitmap[x >> 3] & (1 << (x & 7));
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int x;
    while (cin >> x) setBit(x);

    bool first = true;
    for (int i = 0; i < MAXN; ++i) {
        if (getBit(i)) {
            if (!first) cout << ' ';
            cout << i;
            first = false;
        }
    }
    cout << '\n';
    return 0;
}
